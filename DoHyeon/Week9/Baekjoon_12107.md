```python
import sys

N = int(sys.stdin.readline())
#
# is_it_prime = [True] * 1000001
#
# prime_num = [[1] for _ in range(1000001)]
#
# for i in range(2, N+1):
#     if is_it_prime[i]:
#         for j in range(i*i, N+1, i):
#             is_it_prime[j] = False
#             prime_num[j].append(i)
# 잠시만 잘 생각해보자.
# 1일 경우에는 A가 무조건 진다. 무조건 1을 지워야 하기 때문이다.
# 2라면 A가 이긴다. 1을 지워버리면 B는 2를 무조건 지워야 한다.
# 3역시 A가 이긴다. 3을 지워버리면 B는 2를 무조건 지워야 한다.
# 4도 A가 이긴다. 4를 지워버리면 1 2 4 가 지워지므로 B는 3을 지워야 한다.
# 5는 A가 2를 지워버리면 무슨 수를 써도 B는 이길 수 없다.
# 1은 모든 수의 약수이므로 게임을 이렇게 볼 수 있다.
# 1을 제외하고 게임한다고 가정하면, 1을 제외한 게임에서 A가 승리한다면 그게임은 A의 승리이다.
# 왜냐하면 1은 모든 수의 약수이기 때문에 A가 게임을 시작한 순간 지워졌을 것이기 때문이다.
# 1을 제외한 게임에서 A가 무조건 지는 결과가 나온다면 A는 1을 지워서 해당 턴을 미뤄버리면 그만이다.
# 따라서 N == 1을 제외 무조건 A가 이긴다.

if N == 1:
    print("B")
else:
    print("A")

```

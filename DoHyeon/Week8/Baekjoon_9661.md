```python
import sys
N = int(sys.stdin.readline())
# dp = [-1] * 1001
# # 마지막 돌을 가져가면 지는 게임
# # 1개, 4개, 16개, 64개,..., 4**k개의 선택지가 존재
# # 4**k개를 가져가지 못하면 끝
# # 근데 1개씩은 가져갈 수 있으니 결국 마지막 돌을 가져간 사람이 승리
# dp[1] = 1 # 상근이 승
# dp[2] = 0 # 창영이 승
# dp[3] = 1 # 상근이 승 (1개 씩 가져갈 경우)
# dp[4] = 1 # 상근이 승 (4개 한꺼번에 가져감)
# dp[5] = 0 # 창영이 승 (상근이가 1개를 가져가면 창영이가 4개를, 상근이가 4개를 가져가면 창영이가 1개를 가져가므로 무조건 창영이가 이김)
# dp[6] = 1 # 상근이 승 (상근이가 4개를 가져가면 창영이는 무조건 1개를 가져가므로 상근이가 마지막1개를, 상근이가 1개를 가져가면 창영이가 4개를 가져가든 1개를 가져가든 상근이 승)
# # ... 16 전까지는 dp[i-4] 혹은 dp[i-1] 중 0이 하나라도 있으면 dp[i] = 1
# dp[16] = 1 # 상근이 승 (상근이가 16개를 한번에 가져가면 그만)
# dp[17] = 0 # 창영이 승 (dp[13],dp[1],dp[16] 모두 1이다.)
#
#
# for i in range(5, N+1):
#     if dp[i-1] == 0 or dp[i-4] == 0:
#         dp[i] = 1
#     else:
#         dp[i] = 0
# if N>=16:
#     for i in range(16,N+1):
#         if dp[i-1] == 0 or dp[i-4] == 0 or dp[i-16] == 0 or i == 16:
#             dp[i] = 1
#         else:
#             dp[i] = 0
# if N>=64:
#     for i in range(64,N+1):
#         if dp[i-1] == 0 or dp[i-4] == 0 or dp[i-16] == 0 or dp[i-64] == 0 or i == 64:
#             dp[i] = 1
#         else:
#             dp[i] = 0
# 10110이 반복..?

if N%5 == 0 or N%5 == 2:
    print("CY")
else:
    print("SK")

```
